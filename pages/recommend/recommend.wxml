<view class="coupon">
  <view class="section_gap" wx:for="{{array}}" wx:for-item="item">
    <image class="image_url" mode="widthFix" src="{{item.src}}"></image>
    <view class="section_title">{{item.text}}</view>
    <view class="section_price">{{item.price}}</view>
  </view>
</view>